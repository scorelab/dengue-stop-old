export default theme=>({
    LocationDataWrapper:{
        height: '100%',
        overflowY: 'auto',
        overflowX:'hidden',
    },
    newsContainer:{
        marginTop:theme.spacing(2),
        marginRight:theme.spacing(2)
    }
})